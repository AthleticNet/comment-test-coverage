name: 'Comment Test Coverage'
description: 'Read a Karma Test Coverage json-summary test report and comment stats on to commit'
inputs:
  token:
    description: 'The GitHub authentication token'
    required: true
  body:
    description: 'Contents of the comment'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
branding:
  icon: 'message-square'  
  color: 'gray-dark'